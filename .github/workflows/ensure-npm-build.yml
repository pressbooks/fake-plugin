name: Npm build ⚙️

on:
  pull_request:
    branches: [ dev ]
    paths:
      - "resources/assets/**/*"

jobs:
  update-npm-build:
    uses: pressbooks/reusable-workflows/.github/workflows/npm-build.yml@test-build
    secrets:
      PAT_FOR_GITHUB_ACTIONS: ${{ secrets.PAT_FOR_GITHUB_ACTIONS }}
