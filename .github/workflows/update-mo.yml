name: Update .mo files 🌐

on:
  push:
    branches: [ dev ]
    paths:
      - "languages/*.po"
  workflow_dispatch:

jobs:
  update-mo-files:
    uses: pressbooks/reusable-workflows/.github/workflows/update-mo.yml@updates
