name: Update .pot file 🌐

on:
  pull_request:
    branches: [ dev ]
    paths:
      - '**.php'
      - '**.js'
  workflow_dispatch:

jobs:
  update-npm-build:
    if: ${{ github.actor != 'dependabot[bot]' && github.actor != 'dependabot-preview[bot]' }}
    uses: pressbooks/reusable-workflows/.github/workflows/npm-build.yml@fix/pot-trigger
