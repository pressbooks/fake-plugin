name: Run Standards & Tests

on:
  push:
    branches: [ dev, main ]

jobs:
  trigger_bedrock_updates:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev'
    steps:
      uses: pressbooks/composer-autoupdate-bedrock/.github/workflows/trigger-update.yml@main
      secrets: inherit
